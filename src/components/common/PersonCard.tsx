import * as React from 'react';

const PersonCard: React.FC = () => {
    return (
        <div>
            <p>Person Card component</p>
        </div>
        );
    
};

export default PersonCard;  